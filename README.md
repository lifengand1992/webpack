# webpack
webpack basic demo
webpack 的安装
npm install --save-dev webpack

devServer的安装
npm install --save-dev webpack-dev-server

npm init：新建package.json文件

npm start开始webpack打包

设置devServer后，可以直接运行npm run server，执行自动打开index.html页面，修改data.json数据直接实时刷新数据

